import 'package:flutter/material.dart';

class Appcolor {
  static const primary = Color(0xff003eff);
  static const secondary = Color(0xffbaedfc);
}
//This code defines a class named Appcolor with two static constants: primary and secondary. 
//These constants represent the primary and secondary colors of the app and are defined as Color objects with hexadecimal values. 
//The primary color is a deep blue, while the secondary color is a lighter blue.
//These constants can be accessed and used throughout the app to apply a consistent color theme.